enum PRICING_RULE_TYPE {
  specialPrice = 'special price',
  xForY = 'x for y',
  conditionalSpecialPrice = 'conditional special price'
}

enum ADS_TYPE {
  classic = 'classic',
  standout = 'standout',
  premium = 'premium'
}

export {
  PRICING_RULE_TYPE,
  ADS_TYPE,
}
